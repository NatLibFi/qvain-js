{
  "name": "qvain-validator",
  "version": "0.4.0",
  "description": "A json-schema validator for the Qvain metadata tool",
  "main": "index.js",
  "scripts": {
    "build": "babel --no-babelrc src --out-dir build --presets=env",
    "watch": "npm run build -- -w",
    "test": "mocha --require babel-register",
    "clean": "rm -rf build/"
  },
  "author": "Wouter Van Hemel",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "deep-equal": "^1.0.1",
    "deepmerge": "^2.1.0",
    "json-deep-copy": "^1.0.2",
    "json-pointer": "^0.6.0",
    "vue": "^2.5.16"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.2.0"
  },
  "babel": {
    "presets": ["env"]
  }
}
